#pragma once

// ================================================================
//  SENTINEL — Secrets — TEMPLATE À VERSIONNER
//  Copier ce fichier → secrets.h et remplir les vraies valeurs.
//  Le fichier secrets.h est dans .gitignore et ne sera jamais commité.
// ================================================================

// ─── WiFi ────────────────────────────────────────────────────────
#define WIFI_SSID       "YOUR_WIFI_SSID"
#define WIFI_PASSWORD   "YOUR_WIFI_PASSWORD"

// ─── Broker MQTT ─────────────────────────────────────────────────
// En développement : IP locale du PC faisant tourner Mosquitto
// Exemple : "192.168.1.42"
#define MQTT_BROKER     "192.168.X.X"
#define MQTT_PORT       1883
#define MQTT_USER       ""        // Laisser vide si pas d'authentification
#define MQTT_PASS       ""

// ─── Sécurité IoT ────────────────────────────────────────────────
// Doit correspondre à DEVICE_SHARED_SECRET dans le backend/.env
#define DEVICE_SECRET   "CHANGE_ME_IN_PRODUCTION"
